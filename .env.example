# Database
DB_URL=postgres://username:password@localhost:5432/shopedia?sslmode=disable

# Redis (Queue)
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=your-super-secret-key-min-32-characters

# SMTP (for OTP & password reset emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Server (optional)
PORT=:3000

# Docker PostgreSQL (used by docker-compose)
POSTGRES_USER=shopedia
POSTGRES_PASSWORD=shopedia123
POSTGRES_DB=shopedia
